import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    backGroundColor: {
        backgroundColor: '#FFFFFF'
    },
    flexRow: {
        flexDirection: "row",
        alignItems: "center",
    }
})